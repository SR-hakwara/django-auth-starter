"""Top‑level package namespace for django-auth-basic.

This package is intentionally empty; the repository is not distributed as a
standard installable wheel.  We expose a single attribute here so that
code (or documentation) can reference the current release version.
"""

__version__ = "1.5.0"  